FROM golang:1.25 AS builder

WORKDIR /app
COPY . .
# Production API (Gin + workspace routes). Do not use legacy cmd/main.go here.
RUN go build -o etl ./cmd/api

FROM gcr.io/distroless/base
COPY --from=builder /app/etl /main
CMD ["/main"]
